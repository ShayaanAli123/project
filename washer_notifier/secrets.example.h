// This is a TEMPLATE. Copy this file to `secrets.h` in the same folder,
// then fill in your real values there. `secrets.h` is gitignored and
// will never be pushed to GitHub. `secrets.example.h` (this file) IS
// committed — it just shows the shape of the file with placeholders.

#pragma once

// 2.4 GHz WiFi network name and password.
static const char* BINGU_SSID = "YOUR_WIFI_SSID";
static const char* BINGU_EATS = "YOUR_WIFI_PASSWORD";

// Topic you subscribed to in the ntfy phone app. Must match exactly.
static const char* NTFY_TOPIC = "your-unique-topic-name-here";
